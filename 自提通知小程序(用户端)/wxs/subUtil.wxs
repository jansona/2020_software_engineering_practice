/**
 * 字符串截取函数
 * @param val 初始字符串
 */
var sub = function(val) {
  if (val.length == 0 || val == undefined) {
    return;
  }
  if (val.length > 14) {
    return val.substring(0, 14) + "...";
  } else {
    return val;
  }
}
module.exports.sub = sub;